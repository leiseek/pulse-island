[package]
name = "pulse-reducer"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
pulse-domain = { path = "../pulse-domain" }
pulse-protocol = { path = "../pulse-protocol" }

[dev-dependencies]
pulse-testkit = { path = "../pulse-testkit" }

[lints]
workspace = true
