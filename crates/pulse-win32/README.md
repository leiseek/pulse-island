# pulse-win32

W2 Windows platform primitives for the native shell. This crate starts with pure, testable models for DPI, placement, and hit-testing. It must not contain provider logic, task semantics, Link transport, persistence, network access, SQLite, browser runtimes, or raw provider data.
