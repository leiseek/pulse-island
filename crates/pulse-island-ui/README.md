# pulse-island-ui

Provider-neutral W2 UI view-model crate. It consumes `PresentationPlan` and route labels only; it must not depend on provider adapters, Link transport, persistence, Win32 activation, network, SQLite, browser runtimes, or raw provider data.
