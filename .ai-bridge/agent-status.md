# Agent Status

Updated: 2026-07-08T02:32:23+08:00
Agent: Codex

## Files touched

- `.ai-bridge/current-plan.md`
- `.ai-bridge/agent-status.md`
- `crates/pulse-arbitration/src/lib.rs`
- `crates/pulse-domain/src/lib.rs`
- `crates/pulse-protocol/src/lib.rs`
- `crates/pulse-reducer/src/lib.rs`
- `crates/pulse-testkit/tests/w1_truth_fixtures.rs`
- `.ai-bridge/implementation-diff.patch`
- `docs/pulse-island/W1-GATE-AUDIT.md`
- `crates/pulse-island-ui/Cargo.toml`
- `crates/pulse-island-ui/README.md`
- `crates/pulse-island-ui/src/lib.rs`
- `crates/pulse-win32/Cargo.toml`
- `crates/pulse-win32/README.md`
- `crates/pulse-win32/src/lib.rs`
- `crates/pulse-win32-hwnd/Cargo.toml`
- `crates/pulse-win32-hwnd/src/lib.rs`
- `crates/pulse-win32-hwnd/tests/hwnd_backend_contract.rs`
- `crates/pulse-link-core/Cargo.toml`
- `crates/pulse-link-core/src/lib.rs`
- `crates/pulse-link-core/tests/frame_contract.rs`
- `crates/pulse-link-core/tests/handoff_contract.rs`
- `crates/pulse-link-core/tests/lifecycle_contract.rs`
- `crates/pulse-link-core/tests/island_protocol_contract.rs`
- `crates/pulse-persistence/Cargo.toml`
- `crates/pulse-persistence/src/lib.rs`
- `crates/pulse-persistence/tests/breadcrumb_contract.rs`
- `apps/pulse-link-shim/Cargo.toml`
- `apps/pulse-link-shim/src/lib.rs`
- `apps/pulse-link-shim/src/main.rs`
- `apps/pulse-link-shim/tests/shim_fail_open.rs`
- `apps/pulse-link/Cargo.toml`
- `apps/pulse-link/src/lib.rs`
- `apps/pulse-link/src/main.rs`
- `apps/pulse-link/tests/link_runner_contract.rs`
- `apps/pulse-link/tests/link_scenarios_contract.rs`
- `apps/pulse-link-spike-client/Cargo.toml`
- `apps/pulse-link-spike-client/src/lib.rs`
- `apps/pulse-link-spike-client/src/main.rs`
- `apps/pulse-link-spike-client/tests/spike_client_contract.rs`
- `apps/pulse-island-spike/Cargo.toml`
- `apps/pulse-island-spike/src/lib.rs`
- `apps/pulse-island-spike/src/main.rs`
- `docs/pulse-island/README.md`
- `docs/pulse-island/W2-GATE-AUDIT.md`
- `docs/pulse-island/W3-GATE-AUDIT.md`
- `docs/pulse-island/W4-PROBE-HARNESS-AUDIT.md`
- `docs/pulse-island/25-consistency-closure.md`
- `crates/pulse-win32/tests/link_names_contract.rs`
- `crates/pulse-win32/tests/link_ownership_contract.rs`
- `crates/pulse-win32-link/Cargo.toml`
- `crates/pulse-win32-link/src/lib.rs`
- `crates/pulse-win32-link/tests/link_transport_contract.rs`

## Checks run

- PASS: `cargo fmt --check`
- PASS: `cargo test --workspace`
- PASS: `cargo clippy --workspace --all-targets`
- PASS: `cargo tree --workspace --depth 1`
- PASS: `cargo run -p pulse-island-spike -- S7 --replay`
- PASS: `cargo run -p pulse-island-spike -- --focus-policy`
- PASS: `cargo run -p pulse-island-spike -- --palette-policy`
- PASS: `cargo run -p pulse-island-spike -- --layout-policy`
- PASS: `cargo run -p pulse-island-spike -- --route-policy`
- PASS: `cargo run -p pulse-island-spike -- --lifecycle-policy`
- PASS: `cargo run -p pulse-island-spike -- --hit-test-policy`
- PASS: `cargo run -p pulse-island-spike -- --hotkey-policy`
- PASS: `cargo run -p pulse-island-spike -- --dpi-policy`
- PASS: `cargo run -p pulse-island-spike -- --cache-policy`
- PASS: `cargo run -p pulse-island-spike -- --animation-policy`
- PASS: `cargo run -p pulse-island-spike -- --resource-policy`
- PASS: `cargo run -p pulse-island-spike -- --accessibility-policy`
- PASS: `cargo run -p pulse-island-spike -- --measurement-policy`
- PASS: `cargo run -p pulse-island-spike -- --overlay-policy`
- PASS: `cargo run -p pulse-island-spike -- --window-policy`
- PASS: `cargo run -p pulse-island-spike -- --adapter-plan-policy`
- PASS: `cargo run -p pulse-island-spike -- --adapter-state-policy`
- PASS: `cargo run -p pulse-island-spike -- --adapter-action-policy`
- PASS: `cargo run -p pulse-island-spike -- --adapter-replay-policy`
- PASS: `cargo run -p pulse-island-spike -- --w2-review-ready`
- PASS: `cargo run -p pulse-island-spike -- --w2-review-manifest`
- PASS: `cargo run -p pulse-island-spike -- --truth-priority-policy`
- PASS: `cargo run -p pulse-island-spike -- --surface-handle-policy`
- PASS: `cargo run -p pulse-island-spike -- --architecture-policy`
- PASS: `cargo run -p pulse-island-spike -- --gate-audit`
- PASS: `cargo test -p pulse-win32-hwnd` ran 10 preflight/native-backend/factory-spec/pump-report/hit-test-bridge/mouse-input-bridge/paint-bridge boundary tests.
- PASS: `npx ctx7@latest library "windows-rs" ...` resolved official `/microsoft/windows-rs`, and `npx ctx7@latest docs /microsoft/windows-rs ...` returned current docs.
- PASS: `cargo check --workspace` with rustup's self-contained GNU `dlltool.exe` prepended to PATH.
- PASS: `cmd /c "call \"C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat\" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo check --workspace"`.
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-win32'` ran 28 tests including the two `windows-sys` parity tests.
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-win32-hwnd'` ran 16 tests, compiled the MSVC-only `windows-sys` adapter, created/destroyed a real hidden compact HWND, drained a posted `WM_NULL` without blocking, verified real WndProc `WM_NCHITTEST` results, verified `WM_MOUSEACTIVATE -> MA_NOACTIVATE`, verified content-free `WM_LBUTTONUP` dispatch, and verified content-free `WM_PAINT` dispatch.
- PASS: `cargo test -p pulse-win32 native_adapter_driver`
- PASS: `cargo test -p pulse-win32 native_adapter_commands_carry_backend_payloads_for_visible_plan`
- PASS: `cargo test -p pulse-link-core`
- PASS: `cargo test -p pulse-link-shim`
- PASS: `cargo test -p pulse-persistence --test breadcrumb_contract`
- PASS: `cargo test -p pulse-link --test link_runner_contract`
- PASS: `cargo test -p pulse-link --test link_scenarios_contract`
- PASS: `cargo test -p pulse-link-shim --test shim_fail_open`
- PASS: `cargo test -p pulse-link --test link_native_transport_contract`
- PASS: `cargo test -p pulse-win32-link --test link_transport_contract`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-win32-link windows_sys_os_transport_harness_creates_pipe_mutex_handoff_and_cleans_up'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-win32-link windows_sys_ingress_pipe_round_trips_frame_header_and_ack'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-link windows_sys_ingress_frame_ack_drives_reducer_and_checkpoint'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-link windows_sys_ingress_frame_ack_loop_rejects_bad_frame_without_stopping_reducer'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-link windows_sys_grace_exit_closes_transport_and_leaves_no_child_residue'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-link windows_sys_c0_c9_harness_covers_all_spike_c_scenarios'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo test -p pulse-link-spike-client windows_sys_island_pipe_protocol_loop_handles_attach_delta_and_gap'`
- PASS: `cargo test -p pulse-link-spike-client --test spike_client_contract` ran 6 tests including pipe message-loop startup, attach sequence, delta delivery, and revision-gap recovery.
- PASS: `cargo test -p pulse-island-spike cli_exports_w2_review_manifest_as_machine_checkable_contract`
- PASS: `cargo test -p pulse-island-spike w4_`
- PASS: `cargo run -p pulse-island-spike -- --provider-probe-manifest`
- PASS: `cargo run -p pulse-island-spike -- --provider-probe-report=codex_cli`
- PASS: `cargo run -p pulse-island-spike -- --provider-config-transaction-fixture=claude_code`
- PASS: `cargo run -p pulse-island-spike -- --provider-probe-scorecard`
- PASS: `cargo run -p pulse-island-spike -- --provider-resource-fixture=codex_cli`
- PASS: `cargo run -p pulse-island-spike -- --provider-evidence-register=codex_cli`
- PASS: `cargo run -p pulse-island-spike -- --provider-probe-summary-fixture=claude_code`
- PASS: `cargo run -p pulse-island-spike -- --provider-probe-scorecard=sanitized-fixture`
- PASS: `cargo run -p pulse-island-spike -- --provider-hard-disqualifiers=sanitized-fixture`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_evidence_gap_summary_across_providers_without_live_actions`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-evidence-gap-summary`
- PASS: `cargo run -p pulse-island-spike -- --provider-probe-audit`
- PASS: `cargo run -p pulse-island-spike -- --provider-surface-inventory`
- PASS: `cargo run -p pulse-island-spike -- --provider-probe-readiness`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_full_capability_matrix_without_blank_or_elevated_rows`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-capability-matrix=codex_cli`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_release_label_evaluation_without_elevating_sanitized_fixture`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-release-label-evaluation=sanitized-fixture`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_probe_phase_status_without_executing_live_phases`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-probe-phase-status=claude_code`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_w5_start_preflight_that_blocks_adapter_creation_without_direct_evidence`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_direct_evidence_import_checklist_without_accepting_weak_evidence`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_sanitized_evidence_output_template_without_raw_artifacts`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_release_elevation_preflight_without_promoting_provider`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_w5_observe_adapter_contract_without_creating_adapter`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_authorized_evidence_runbook_without_executing_provider_actions`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_sanitized_evidence_bundle_validator_without_importing_artifacts`
- PASS: `cargo test -p pulse-island-spike cli_exports_w4_official_evidence_source_locator_without_raw_docs_or_claims`
- PASS: `cargo test -p pulse-island-spike w4_` ran 36 W4 tests.
- PASS: `cargo run -q -p pulse-island-spike -- --provider-w5-start-preflight`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-official-evidence-source-locator=codex_cli`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-direct-evidence-import-checklist=claude_code`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-authorized-evidence-runbook=codex_cli`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-sanitized-evidence-output-template=codex_cli`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-sanitized-evidence-bundle-validator=claude_code`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-release-elevation-preflight=codex_cli`
- PASS: `cargo run -q -p pulse-island-spike -- --provider-w5-observe-adapter-contract=claude_code`
- PASS: stale-boundary scan across `.ai-bridge` and `docs/pulse-island` found no legacy-boundary markers and no stale W4 25-35 test-count text.
- PASS: `cargo run -p pulse-island-spike -- --provider-local-environment-manifest=read-only-fixture`
- PASS: `cargo run -p pulse-island-spike -- --provider-live-probe-dry-run=read-only-fixture`
- PASS: read-only `Get-Command codex, claude, antigravity`
- PASS: read-only `codex --version`
- PASS: read-only `claude --version`
- INFO: read-only `antigravity --version` not found on PATH
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo check -p pulse-link-shim -p pulse-link'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo check -p pulse-persistence -p pulse-link'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo check -p pulse-link'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo check -p pulse-win32-link -p pulse-link'`
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo check -p pulse-win32-link -p pulse-link-spike-client'`
- PASS: `cargo test -p pulse-link-core --test lifecycle_contract` ran 4 lifecycle transition tests.
- PASS: `cargo test -p pulse-link-core --test island_protocol_contract` ran 2 fake Island protocol tests.
- PASS: `cargo test -p pulse-link-core --test handoff_contract` ran 2 initial handoff safety tests.
- PASS: `cargo test -p pulse-persistence --test breadcrumb_contract` ran 4 bounded breadcrumb tests.
- PASS: `cargo test -p pulse-link --test link_runner_contract` ran 3 pure Link runner tests.
- PASS: `cargo test -p pulse-link --test link_scenarios_contract` ran 10 pure synthetic C0-C9 scenario tests.
- PASS: `cargo test -p pulse-link-spike-client --test spike_client_contract` ran 3 fake Island client tests.
- PASS: `cargo test -p pulse-win32 --test link_names_contract` ran 3 content-free Link object naming tests.
- PASS: `cargo test -p pulse-win32 --test link_ownership_contract` ran 4 pure single-instance ownership tests.
- PASS: `cargo test -p pulse-win32-link --test link_transport_contract` ran 6 safe/native Link transport boundary tests.
- PASS: `cmd.exe /d /s /c 'call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 >nul && rustup run stable-x86_64-pc-windows-msvc cargo check -p pulse-win32-link'`.
- INFO: `windows-window v0.0.0` was probed as a possible safe wrapper but does not expose the documented `Window` API; it is not used.

## Results

Continued the canonical W0-W6 sequence through the current W4 Provider Probe Harness boundary. W0/W1/W2/W3 are accepted regression evidence, not active task queues.

- Added a Safe Mode Shim ingress decision seam: Safe Mode does not wake Link, does not forward ingress, and exits successfully.
- Added resource-stall truth state and arbitration tier 6, below Fuel risk and above running.
- Prevented process-only observation from downgrading already attached/running task truth.
- Added explicit breadcrumb retention decisions for Minimal, Strict, and PassiveOnly privacy profiles while keeping the older bool compatibility field.
- Added feature capability declarations for waiting and route evidence without upgrading provider release status.
- Downgraded revoked verified-limit claims without inventing running state.
- Allowed stale/degraded waiting to recover to running on fresh activity while preserving fresh waiting.
- Protected failed terminal summaries from late completed events.
- Added process-exit-without-terminal-evidence semantics: offline, not completed/failed.
- Expanded privacy hardening for `password`, `credential`, and `bearer` markers at bounded-text and frame-preflight boundaries.
- Added `docs/pulse-island/W1-GATE-AUDIT.md` to map W1 requirements to concrete test evidence.
- Added route-kind-aware label policy so Exact/Strong/Useful labels can represent the full `05`/`25` vocabulary while rejecting mismatched strength/kind combinations.
- Added task token sample safety in `pulse-fuel`: duplicate samples and counter resets produce zero burn, never negative or duplicate deltas.
- Advanced active boundary to W2 after W1 audit reached ready-for-review.
- Added `pulse-island-ui` W2 view-model seam with `SignalViewModel`, `PresentationPlanSource`, and `MockPresentationPlanSource`.
- Added `pulse-win32` W2 pure platform primitives for hit-testing and DPI scaling without linking Windows APIs yet.
- Added Spike A deterministic mock scenario catalog S0-S8 in `pulse-island-ui`.
- Added W2 shell visibility model: compact hidden for idle/immersive, motion policy stopped under immersive, reduced-motion and high-contrast switches carried as environment policy.
- Added compact Signal truth-priority policy in `pulse-island-ui`: the primary story remains the `PresentationPlan` primary, timer-based task rotation is forbidden, and trustworthy low-Fuel treatment remains a secondary Fuel Thread that cannot override task state.
- Added Peek/Focus derivation from deterministic mock scenarios while preserving route labels and keeping P0 provider controls empty.
- Added `pulse-win32` pure placement clamp primitives for work-area/display-topology changes without real HWND or Windows API linkage.
- Added `apps/pulse-island-spike` as the disposable Spike A scenario runner app crate.
- Added `SpikeScenarioHarness` to list S0-S8, run deterministic snapshots, and replay S7 rapid state changes while preserving `window_generation=1`.
- Added dependency-free CLI behavior for `pulse-island-spike`: no args lists scenarios, `S0`-`S8` runs a scenario, and `--replay` exports deterministic frame lines.
- Added pure W2 focus/visibility interaction state in `pulse-island-ui`: passive plan updates do not steal focus, compact clicks open Peek without focus theft, Palette shortcut explicitly focuses Command Palette, Peek row clicks explicitly focus Focus Card, and Escape closes focused surfaces.
- Added `pulse-island-spike --focus-policy` diagnostic export for repeatable focus-policy checks before real Win32 wiring.
- Added `pulse-island-spike --truth-priority-policy` diagnostic export for repeatable no-timer-rotation and Fuel-secondary checks.
- Added W2 accessibility and motion policy models in `pulse-island-ui`: accessible Signal names with non-color state labels, reduced-motion animation constraints, high-contrast token selection, and deterministic Peek/Palette keyboard navigation.
- Added `pulse-island-spike --accessibility-policy` diagnostic export for reduced-motion, high-contrast, accessible-label, and keyboard navigation checks.
- Added the W2 `PresentationPlanSource::subscribe` seam and mock source delivery path without introducing Link, IPC, or provider dependencies.
- Added P0 Command Palette command modeling for the initial navigation commands from `09`, explicitly marking all as non-provider-control and non-high-risk.
- Added `pulse-island-spike --palette-policy` diagnostic export for repeatable Command Palette policy checks.
- Added compact Signal layout policy in `pulse-island-ui` to preserve state glyph and subject under width pressure while hiding reason, active count, and secondary Fuel in priority order.
- Added `pulse-island-spike --layout-policy` diagnostic export for repeatable compact-width checks.
- Added W2 mock route-label fidelity coverage so Exact, Strong, Useful, and Weak labels are all rendered through Signal/Peek/Focus view models.
- Added `pulse-island-spike --route-policy` diagnostic export for repeatable route-label coverage checks.
- Added pure shell surface lifecycle counters in `pulse-island-ui` to prove 1,000 repeated Peek/Focus/Palette cycles keep `window_generation=1`, leave no active transient surfaces, and cap active transient surfaces at one.
- Added `pulse-island-spike --lifecycle-policy` diagnostic export for repeatable surface lifecycle checks.
- Added focused-surface handle stability reporting in `pulse-island-ui`: after 1,000 Peek and 1,000 Focus open/close cycles, diagnostic USER/GDI handle snapshots must show zero growth and no active transient surfaces.
- Added `pulse-island-spike --surface-handle-policy` diagnostic export for repeatable Peek/Focus handle-stability checks.
- Added pure Win32 `WM_NCHITTEST` result-code mapping in `pulse-win32`: transparent margin maps to `HTTRANSPARENT`, client to `HTCLIENT`, drag grip to `HTCAPTION`, and outside to `HTNOWHERE`.
- Added `pulse-island-spike --hit-test-policy` diagnostic export for repeatable transparent/client/drag/outside hit-test checks before real WndProc wiring.
- Added pure Command Palette hotkey policy in `pulse-win32`: default Ctrl+Shift+Space registration chord and disabled-state no-registration behavior.
- Added Palette invocation policy in `pulse-island-ui`: disabled shortcuts do nothing, immersive suppression blocks Palette unless explicitly permitted, and allowed shortcuts focus Command Palette.
- Added `pulse-island-spike --hotkey-policy` diagnostic export for repeatable hotkey registration and Palette invocation checks.
- Added monitor-aware logical placement in `pulse-win32`: saved per-monitor logical coordinates resolve through current monitor DPI, offset to the monitor work area, clamp after topology changes, and fall back deterministically when a monitor disappears.
- Added Windows text-scale-aware sizing in `pulse-win32`, preserving non-zero text dimensions under DPI/text-scale combinations.
- Added `pulse-island-spike --dpi-policy` diagnostic export for repeatable monitor fallback, DPI placement, and text-scaling checks.
- Added W2 render-cache invalidation policy in `pulse-island-ui`: DPI, theme, font, and state-layout changes invalidate the correct bounded caches; text layout cache is capped and excludes task content.
- Added `pulse-island-spike --cache-policy` diagnostic export for repeatable render-cache bound and invalidation checks.
- Added Gate A measurement policy scaffold in `pulse-island-ui`: required metric names/targets/comparators, diagnostic-metadata-only flag, and explicit task-content exclusion.
- Added a pure Gate A measurement fixture harness in `pulse-island-ui`: diagnostic samples, P95/average/max aggregation by metric type, missing-metric failure, and overall pass/fail report.
- Extended `pulse-island-spike --measurement-policy` to export a deterministic sample report proving all Gate A metrics are present and passing under the mock fixture.
- Added diagnostics-only performance overlay modeling in `pulse-island-ui`: normal mode hides the overlay, diagnostics mode renders only Gate A metric metadata, and task content remains excluded.
- Added `pulse-island-spike --overlay-policy` diagnostic export for repeatable overlay visibility and content-boundary checks.
- Added static render policy in `pulse-island-ui` to forbid app-side frame loops in idle/static states while allowing compositor-only animation classification.
- Added `pulse-island-spike --measurement-policy` diagnostic export for Gate A target schema and static render policy.
- Added pure compositor animation-class policy in `pulse-island-ui` for Arrival, StateTransition, AttentionPulse, Expansion, FuelCue, and Completion; W2 plans are compositor-owned, forbid app-side frame loops, bound attention pulses, settle to static state, and honor reduced/stopped motion.
- Added `pulse-island-spike --animation-policy` diagnostic export for repeatable animation-class checks before real compositor wiring.
- Added pure render-resource policy and report evaluation in `pulse-island-ui`: one shared D3D device, no per-task full render surfaces, virtualized Focus Card rows, and zero device/surface/D3D/handle growth across the 1,000-transition W2 leak check.
- Added `pulse-island-spike --resource-policy` diagnostic export for repeatable resource-stability checks before real D3D/DirectComposition wiring.
- Added pure `pulse-win32` compact window style policy for popup/topmost/toolwindow/noactivate, Alt+Tab exclusion, and non-permanent click-through behavior.
- Added MSVC-only `windows-sys 0.61.2` dev parity tests in `pulse-win32` to prove pure style, hit-test, and hotkey constants match current Rust for Windows bindings without production HWND calls or unsafe code.
- Added pure `pulse-win32` immersive window policy for normal versus fullscreen suppression without fighting exclusive fullscreen surfaces.
- Added `pulse-island-spike --window-policy` diagnostic export for style and immersive policy checks before real HWND wiring.
- Added pure `pulse-win32` `NativeWindowAdapterPlan` composition for the next W2 adapter slice: visible and immersive frames keep one compact window, avoid focus activation, avoid hidden-window destruction, avoid missed-animation replay, and derive style/hit-test/hotkey/placement from existing pure policies.
- Added `pulse-island-spike --adapter-plan-policy` diagnostic export for repeatable native-adapter-plan checks before real HWND wiring.
- Added pure `pulse-win32` `NativeWindowAdapterState` accumulation for the next W2 adapter slice: repeated visible frames and visible/immersive/visible transitions create one compact window generation, avoid destruction/recreation, avoid activation attempts, and register the Palette hotkey once without churn.
- Added `pulse-island-spike --adapter-state-policy` diagnostic export for repeatable adapter-state checks before real HWND wiring.
- Added pure `pulse-win32` `NativeWindowAdapterAction` diffs for the next W2 adapter slice: first visible frame produces ordered create/style/update-hit-test/move/register/show/topmost actions, repeated identical visible frames produce no actions, immersive suppression produces hide/clear-topmost without destroy or hotkey churn, hit-test layout changes produce only `UpdateHitTestLayout`, placement changes produce only `MoveResize`, style changes produce only `ApplyWindowStyles`, and disabled hotkey policy produces only `UnregisterHotkey`.
- Added safe `pulse-win32` `NativeWindowAdapterDriver` and `NativeWindowActionSink` seam for the next W2 adapter slice: ordered action diffs can drive a backend, and pure state advances only after every sink action succeeds.
- Added payload-bearing `pulse-win32` `NativeWindowAdapterCommand` diffs and `NativeWindowCommandSink` seam so a future HWND backend receives style bits, hit-test layout, placement, and hotkey payloads; pure state still advances only after every command succeeds.
- Added `pulse-win32-hwnd` as the explicit HWND unsafe boundary crate. It contains safe preflight logic plus an MSVC-only `windows-sys` native API adapter.
- Added `HwndCommandPreflightSink` contract tests proving ordered visible-plan commands are accepted, move/resize before compact-window creation is rejected, invalid hotkey payloads are rejected, and only validated commands are recorded.
- Added `HwndNativeBackend`, `HwndCompactWindowFactory`, `HwndNativeApi`, and `RawHwnd` so payload commands can reach native APIs only after preflight and HWND creation.
- Added MSVC-only `WindowsSysHwndApi`, wrapping `SetWindowLongPtrW`, `SetWindowPos`, `ShowWindow`, `RegisterHotKey`, and `UnregisterHotKey` behind the safe `HwndNativeApi` trait.
- Added `CompactWindowClassSpec` and MSVC-only `WindowsSysCompactWindowFactory`, registering a content-free class, calling `CreateWindowExW` for a hidden popup/toolwindow/no-activate compact HWND, and destroying it with `DestroyWindow`.
- Added `HwndMessagePumpBudget`, `HwndMessagePumpReport`, and MSVC-only `WindowsSysMessagePump`, draining pending compact-HWND messages with bounded nonblocking `PeekMessageW(PM_REMOVE)`, `TranslateMessage`, and `DispatchMessageW`.
- Added `HwndHitTestBridge`; MSVC WndProc now handles `WM_NCHITTEST` by converting screen coordinates with `ScreenToClient` and returning pure `Win32HitTestCode` values from cached content-free geometry.
- Added compact HWND activation-edge handling; MSVC WndProc now returns `MA_NOACTIVATE` for `WM_MOUSEACTIVATE`, verified against a real hidden HWND.
- Added content-free compact mouse input dispatch; `HwndMouseInputBridge` maps only client-body `WM_LBUTTONUP` to `CompactPrimaryClick`, transparent/drag/outside points dispatch nothing, and MSVC WndProc tests drain that event from a real hidden HWND.
- Added content-free compact paint readiness dispatch; `HwndPaintBridge` maps `WM_PAINT` to `CompactRepaintRequested`, MSVC WndProc tests drain that render event from a real hidden HWND, and WndProc validates the paint region.
- Added `pulse-island-spike --adapter-action-policy` diagnostic export for repeatable adapter-action checks before real HWND wiring.
- Added `pulse-island-spike --adapter-replay-policy` diagnostic export for repeatable S7/S8 adapter replay checks before real HWND wiring: one window generation, one create, zero destroy, zero activation, one hotkey registration, no hotkey unregister, S7 bounded action budget (`total_actions=9`, `max_actions_per_frame=7`), and immersive hide/clear-topmost actions.
- Added `pulse-island-spike --w2-review-ready` diagnostic export for machine-checkable W2 review readiness; current status is accepted for W3 sequencing with `w3_ready=true`.
- Added `pulse-island-spike --w2-review-manifest` diagnostic export for a stable machine-checkable W2 review contract: manifest version, W2 package/scope/status, Gate A counts, adapter readiness, evidence doc, zero forbidden dependency hits, and W3 authorized scope.
- Added `pulse-island-spike --architecture-policy` diagnostic export for W2 manifest architecture checks: zero forbidden provider-adapter, SQLite, browser/WebView/Tauri/Electron dependency hits across UI/spike/win32/hwnd-boundary manifests, the mock `PresentationPlanSource` seam remains replaceable, the HWND boundary manifest is present, the HWND native API adapter is present, the HWND create-window factory is present, the HWND message pump is present, WndProc hit-test integration is present, WndProc mouse-activation rejection is present, content-free WndProc mouse dispatch is present, and content-free WndProc paint dispatch is present.
- Added `pulse-island-spike --gate-audit` diagnostic export aggregating the 21 Spike A acceptance checklist items into functional/window/performance/architecture counts; current status reports `w3_ready=true w2_review=accepted`.
- Increased `w1_truth_fixtures` coverage to 23 tests.
- Added `docs/pulse-island/W2-GATE-AUDIT.md` to map all 21 Spike A checklist items to deterministic evidence; current status is accepted for W3 start.
- Added the W1/W2 gate audit docs to the `docs/pulse-island/README.md` execution and quality gate map.
- Ran the preflight for the next possible W2 native adapter slice: ctx7 successfully fetched current `/microsoft/windows-rs` docs; GNU builds can find `dlltool.exe` when rustup's self-contained path is prepended; MSVC builds pass when invoked through `VsDevCmd.bat`.
- Removed the stale W3 blocker from W1/W2 docs and `pulse-island-spike` diagnostics; W2 is now marked `accepted_for_w3`, and `--gate-audit` reports `w3_ready=true w2_review=accepted`.
- Advanced the active boundary to W3 Link / Shim / Drop Mode under `14-spike-c-link-transport-drop-mode.md`.
- Added `pulse-link-core` with content-free local frame header encoding/decoding, Spike C payload caps, and rejection before payload parsing.
- Added `pulse-link-shim` with Safe Mode no-op behavior and fail-open bounded input preflight for oversized/forbidden/malformed input and Link delivery failure.
- Added `pulse-link-core` Spike C lifecycle reducer covering wake/ready, active, Island-active, Drop Mode, grace, checkpoint, and exit transitions.
- Added `pulse-persistence` bounded breadcrumb abstraction with active/recent-terminal caps, task/snapshot size caps, lifecycle bucket validation, and complete-replacement in-memory checkpointing.
- Added `pulse-link-core` fake Island protocol session for Hello/GetSnapshot/Subscribe, monotonic deltas, and revision-gap full snapshot recovery.
- Added `pulse-win32` Link local object naming model for mutex, ingress pipe, Island pipe, and ready event; derived names are stable per install/user/session/protocol and do not expose raw SID/session/install inputs.
- Added `pulse-win32` pure single-instance ownership model covering first owner, existing owner reuse, bounded stale mutex/pipe retry, fail-open stale exhaustion, and per-session scoping.
- Added `pulse-link-core` initial handoff launch plan proving the first event is delivered through inherited stdin and not command-line/environment/temp-file metadata, with oversized Hook payload rejection before launch planning.
- Added `pulse-win32-link` as the W3 Link transport unsafe-boundary crate, with safe preflight, a native backend executor, raw handle wrappers, inherited handoff pipe handle modeling, and an MSVC-only `windows-sys` adapter for mutex, named pipes, anonymous handoff pipe, and handle close.
- Added `apps/pulse-link` pure synthetic runner that applies admitted events through the reducer, updates lifecycle state, and checkpoints active/recent-terminal memory breadcrumbs.
- Added `apps/pulse-link-spike-client` fake Island client wrapper that performs Hello/GetSnapshot/Subscribe attach and reports delta acceptance or full-snapshot recovery needs.
- Added `apps/pulse-link` pure synthetic C0-C9 scenario harness covering fail-open shim paths, single-Link race semantics, bounded Drop Mode breadcrumbs, Island attach/detach/reattach, restart recovery as degraded, accelerated grace exit, and event-storm caps. C1/C2 now use the pure ownership/handoff models instead of bare launch counters.
- Added `docs/pulse-island/W3-GATE-AUDIT.md` to track W3 progress.
- Added native W3 transport seams in `apps/pulse-link-shim` and `apps/pulse-link`, backed by `pulse-win32-link`: shim reuses acknowledged existing ingress, creates an inherited handoff pipe for first wake, fails open on setup failure, and keeps payload out of argv/env/temp metadata; Link startup creates mutex, ingress pipe, and Island pipe before reporting ready.
- Updated `docs/pulse-island/25-consistency-closure.md`, `24-implementation-work-packages.md`, `README.md`, `W3-GATE-AUDIT.md`, and `.ai-bridge/current-plan.md` during the W3 transition; W3 has since been accepted for W4, so W0/W1/W2/W3 are regression evidence, not active task queues.
- Added `pulse-persistence` file-backed atomic breadcrumb store with complete replacement snapshot writes, same-directory temporary file cleanup, bounded load/checkpoint behavior, missing-file empty snapshot behavior, and oversized-checkpoint rejection that leaves the previous valid snapshot intact.
- Made `apps/pulse-link` runtime accept an injected `BreadcrumbStore`; contract coverage now proves file-backed checkpointing and restart recovery as degraded state.
- Added `apps/pulse-link` Drop Mode grace driver with Spike C fixed 90-second deadline, caller-owned clock, new-event grace cancellation, final checkpoint write at expiry, and C8 scenario coverage through the driver instead of manual lifecycle forcing.
- Added `pulse-win32-link` native shutdown cleanup for handoff write/read handles, Island pipe, ingress pipe, and mutex in reverse ownership order, retaining failed close handles for retry diagnostics.
- Added `apps/pulse-link` native transport runtime seam that retains startup handles and invokes shutdown cleanup after final checkpoint.
- Added `pulse-win32-link` Island client pipe connection preflight/native seam and `apps/pulse-link-spike-client` pipe connection wrapper before the pure fake session attach flow.
- Added `apps/pulse-link-spike-client` pipe-backed fake Island message loop seam requiring a connected Island pipe before startup and handling Hello, snapshot, subscribe, monotonic delta, and revision-gap recovery.
- Renamed the W2 manifest's provider-work field from `blocked_work` to `later_gated_work` so refreshed status docs do not make W2 look like the active blocker.
- Added `pulse-win32-link` MSVC-only OS-backed transport smoke harness that creates a real scoped mutex, ingress pipe, Island pipe, inherited handoff pipe, connects a fake Island client, and closes all six owned handles without residue.
- Added `pulse-win32-link` MSVC-only real ingress named-pipe frame/ack harness. It round-trips an encoded frame header from client to server, returns a one-byte ack, and closes client/server/mutex handles.
- Added `pulse-link` MSVC-only OS-backed ingress reducer harness. It reuses the real ingress frame/ack harness, decodes a content-free Hook frame header, rejects non-Hook or payload-bearing input before reducer mutation, applies a synthetic reducer event, writes a complete replacement checkpoint, and verifies native handle cleanup.
- Added `pulse-win32-link` and `pulse-link` MSVC-only OS-backed multi-frame ingress reducer loop. It acknowledges multiple frames over one named-pipe connection, rejects malformed headers before reducer mutation, continues processing later valid frames, writes checkpoints only for accepted frames, and verifies native handle cleanup.
- Added `pulse-win32-link` and `pulse-link-spike-client` MSVC-only OS-backed Island protocol loop. It round-trips content-free request/response bytes over one Island named-pipe connection, binds the loop to fake Island Hello/GetSnapshot/Subscribe/delta/gap recovery, and verifies native handle cleanup.
- Added `pulse-link` MSVC-only OS-backed C8 residue harness. It starts real Link transport handles, drives terminal synthetic reducer state through grace expiry, writes the final checkpoint, reaches `NotRunning`, closes mutex/ingress/Island handles, and verifies a short-lived child process exits with zero child residue.
- Added `pulse-link` MSVC-only OS-backed Spike C C0-C9 aggregate harness. It covers all ten Spike C scenarios, binds transport-specific paths to real mutex/named-pipe/handoff/residue evidence, preserves fail-open/provider-neutral behavior, and verifies zero retained native handles across OS-backed slices.
- Advanced the active boundary to W4 Provider Probe Harness in read-only capability-discovery mode.
- Added `pulse-island-spike --provider-probe-manifest` for a machine-checkable W4 manifest covering Codex CLI, Claude Code, and Antigravity as `not_probed`.
- Added `pulse-island-spike --provider-probe-report=<provider>` for sanitized per-provider read-only inventory reports with all capability rows at `not_probed`.
- Added `pulse-island-spike --provider-config-transaction-fixture=<provider>` for synthetic install/update/uninstall transaction readiness without real provider config reads or writes.
- Added `pulse-island-spike --provider-probe-scorecard` for first-adapter scorecard aggregation that selects no adapter before probe evidence exists.
- Added `pulse-island-spike --provider-resource-fixture=<provider>` for synthetic resource measurement categories without live provider probing or budget claims.
- Added `pulse-island-spike --provider-evidence-register=<provider>` for sanitized evidence-register summaries without raw source locations, provider content, provider configuration, or capability claims.
- Added `pulse-island-spike --provider-probe-summary-fixture=<provider>` for sanitized probe-result summaries with no raw payload retention.
- Added `pulse-island-spike --provider-probe-scorecard=sanitized-fixture` for sanitized-summary scoring while keeping W5 adapter creation unauthorized.
- Added `pulse-island-spike --provider-hard-disqualifiers=sanitized-fixture` for W4 hard-gate evaluation that blocks W5 when full evidence is missing.
- Added `pulse-island-spike --provider-evidence-gap-summary` for aggregate missing direct gate counts across all three providers. Current summary reports 21 missing direct gates, keeps W4 incomplete, and keeps W5 blocked without executing live actions.
- Added `pulse-island-spike --provider-probe-audit` for aggregate W4 scaffold/audit status.
- Added `pulse-island-spike --provider-surface-inventory` for Probe Card declared provider surfaces without live claims.
- Added `pulse-island-spike --provider-probe-readiness` for W4 scaffold readiness plus remaining live probe/W5 gates.
- Ran a read-only local CLI preflight: Codex CLI `0.142.4` and Claude Code `2.1.195` are present; `antigravity` is not found on PATH. This was recorded only as environment-manifest evidence.
- Added `pulse-island-spike --provider-local-environment-manifest=read-only-fixture` for sanitized local command presence/version categories without paths, config, account data, network, or support claims.
- Added `pulse-island-spike --provider-live-probe-dry-run=read-only-fixture` for dry-run-approved read-only probe actions without executing provider tasks.
- Added `pulse-island-spike --provider-capability-matrix=<provider>` for the full `15-provider-capability-probe.md` matrix scaffold: all 15 template capabilities are present as `not_probed`, no blank cells, no support labels, no winner, no raw provider content/configuration, and W5 remains unauthorized.
- Added `pulse-island-spike --provider-release-label-evaluation=sanitized-fixture` for release-label eligibility evaluation from sanitized fixture state only. Elevated labels stay blocked until direct gates exist, so sanitized fixture scores cannot authorize provider support or W5.
- Added `pulse-island-spike --provider-probe-phase-status=<provider>` for a P0-P8 phase status matrix. P0 is read-only scaffolded; P1-P8 remain not executed and require authorization, with no provider task start or config read/write.
- Added `pulse-island-spike --provider-direct-gate-packet=<provider>` for per-provider direct evidence packet scaffolding without live provider execution.
- Added `pulse-island-spike --provider-w4-completion-gate` for the W4 completion/W5 start gate; it keeps W4 incomplete and W5 blocked until direct evidence exists.
- Added `pulse-island-spike --provider-w5-start-preflight` for a machine-checkable W5 start decision. It blocks provider adapter creation with `w5_start_allowed=false`, `selected_provider=none`, and `w5_adapter_creation_authorized=false` until a provider reaches `supported_observe` through direct evidence.
- Added `pulse-island-spike --provider-official-evidence-source-locator=<provider>` for the W4 official source locator scaffold required by `15-provider-capability-probe.md`. It names required source metadata fields while retaining no raw source locations or raw documentation and creating no capability claim.
- Added `pulse-island-spike --provider-direct-evidence-import-checklist=<provider>` for direct-evidence import acceptance rules. It rejects sanitized fixtures, read-only version evidence, raw provider content, and raw provider configuration as release-elevating inputs while keeping W5 blocked.
- Added `pulse-island-spike --provider-authorized-evidence-runbook=<provider>` for the authorized direct-evidence runbook scaffold. It defines manual authorized steps for synthetic workspace preparation, local-only provider config backup, Probe Card phase execution, direct gate artifact collection, required redaction, and release-elevation preflight while executing no provider action and keeping W5 blocked.
- Added `pulse-island-spike --provider-sanitized-evidence-output-template=<provider>` for sanitized repo artifact templates after authorized direct evidence redaction. It permits only content-free reports/matrices/fixtures/category results/limitations/decision logs and blocks raw prompts, transcripts, provider config, source, and credentials from the repository.
- Added `pulse-island-spike --provider-sanitized-evidence-bundle-validator=<provider>` for the sanitized bundle validator scaffold. It requires repo-safe artifacts, rejects raw prompts/transcripts/provider config/customer source/credentials/terminal buffers/private endpoint traffic, does not import artifacts, and keeps direct evidence plus W5 authorization unclaimed.
- Added `pulse-island-spike --provider-release-elevation-preflight=<provider>` for per-provider `supported_observe` release elevation checks. It currently blocks elevation because direct evidence is missing and hard disqualifiers are uncleared, so W5 adapter creation stays unauthorized.
- Added `pulse-island-spike --provider-w5-observe-adapter-contract=<provider>` for the future W5 Observe adapter contract scaffold. It does not create an adapter, requires a future `supported_observe` release label, limits allowed observe capabilities, excludes external session control/approval UI/transcript parsing/raw-prompt titles/unsupported route or terminal claims/unscoped Fuel, and keeps W5 adapter creation unauthorized.
- Added `docs/pulse-island/W4-PROBE-HARNESS-AUDIT.md` to track W4 evidence and remaining W4 work.

## Current Gates

- W3 has accepted evidence for W4 provider-probe start.
- W4 Provider Probe Harness is now the active work boundary in read-only capability-discovery mode.
- W4 currently has manifest/report scaffolding, synthetic config transaction fixtures, no-selection scorecard aggregation, resource category fixtures, sanitized evidence-register summaries, official evidence source locator scaffolds, sanitized probe summaries, sanitized-summary scorecard scoring, hard-disqualifier evaluation, aggregate evidence gap summary, aggregate audit output, surface inventory export, readiness/remaining-gates output, read-only local CLI environment preflight evidence, a sanitized local environment manifest, read-only live probe dry-run output, a read-only local probe runner that records only version/environment categories, sanitized read-only local probe summaries, a planned-only resource measurement matrix, Probe Card execution plans, probe phase status matrices, evidence retention policy, live authorization preflight, missing-capability rationale, release decision logs, full capability matrix scaffolds, release-label evaluation blocking, per-provider direct gate packets, direct-evidence import checklists, authorized evidence runbooks, sanitized evidence output templates, sanitized evidence bundle validators, release elevation preflights, a W5 observe adapter contract scaffold, a W4 completion gate, and a W5 start preflight that keep W5 blocked until direct evidence selects a supported provider.
- Provider adapters, live Hook installation, provider config mutation, real provider process control, real Fuel collection, and production route activation remain later-gated by W4/W5+.

## Review notes

- No provider adapters, provider configuration edits, live Hooks, network, SQLite, transcript/session parsing, browser runtime, or generic utils crate were added.
- Current OS-backed W3 coverage proves object creation/client connection/cleanup, single-frame ingress acknowledgement driving synthetic reducer/checkpoint state, multi-frame ingress rejection/continuation semantics, OS-backed Island protocol request/response flow, C8 grace-exit transport/child residue cleanup, and aggregate C0-C9 coverage. W4 provider probe is active.
- Current W2 runner remains provider-neutral and pure-testable. It uses only mocked `PresentationPlan` input and workspace-internal dependencies.
- Production `windows-sys` HWND calls are isolated in `pulse-win32-hwnd` and compiled only on MSVC Windows targets.
- Compact HWND creation, bounded nonblocking message drain, WndProc hit-test, WndProc no-activate mouse activation, content-free mouse input dispatch, and content-free paint readiness dispatch are implemented; Direct2D/DirectComposition drawing has not been added yet.
