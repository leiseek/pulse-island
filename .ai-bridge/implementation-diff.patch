Active handoff diff summary for 2026-07-08T00:35:00+08:00.

For the full working-tree diff, run:

```text
git diff
```

Current handoff focus:

- W3 remains the active implementation boundary.
- W0/W1/W2 are accepted sequencing evidence and regression coverage, not active implementation queues.
- `pulse-link-spike-client` now includes a pipe-backed fake Island message loop seam requiring Island pipe connection before startup.
- `pulse-win32-link` now includes an MSVC-only OS-backed transport smoke harness for real mutex, ingress pipe, Island pipe, inherited handoff pipe, Island client connection, and six-handle cleanup.
- `pulse-win32-link` now includes an MSVC-only real ingress named-pipe frame/ack harness for a single encoded frame header.
- `pulse-link` now includes an MSVC-only OS-backed ingress reducer harness: the real ingress frame/ack path accepts a content-free Hook frame header, drives a synthetic reducer event, writes a checkpoint, and closes native handles.
- `pulse-win32-link` and `pulse-link` now include an MSVC-only OS-backed multi-frame ingress reducer loop: multiple frames are acknowledged on one named-pipe connection, malformed headers are rejected before reducer mutation, later valid frames continue, and accepted frames checkpoint synthetic reducer state.
- `pulse-win32-link` and `pulse-link-spike-client` now include an MSVC-only OS-backed Island protocol loop: request/response bytes round-trip on one Island named-pipe connection and bind to fake Island Hello/GetSnapshot/Subscribe/delta/gap recovery.
- `pulse-link` now includes an MSVC-only OS-backed C8 residue harness: grace expiry writes the final checkpoint, reaches `NotRunning`, closes native transport handles, and observes zero child-process residue.
- `pulse-link` now includes an MSVC-only OS-backed Spike C C0-C9 aggregate harness covering all ten scenarios while tying transport-specific paths to real mutex/named-pipe/handoff/residue evidence.
- The W2 review manifest now emits `later_gated_work=...` for provider work that belongs to later gates.
- W3 has accepted evidence for W4 provider-probe start; the next work is W4 read-only capability discovery with live Hooks/config mutation/provider adapters still gated.
